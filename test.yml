Clone:
  title: Running docker image
  type: git-clone
  arguments:
    repo: 'golemcli/otohits'
    git: github
    revision: 'master'


Docker-Build:
  title: Building docker image
  type: build
  working_directory: '${{main_clone}}'
  arguments:
    image_name: 'otohits/latest'
    tag: master
